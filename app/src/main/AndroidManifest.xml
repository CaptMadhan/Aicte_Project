<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aicte">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aicte">
        <activity android:name=".Employer_Application"></activity>
        <activity android:name=".Job_Application"></activity>
        <activity android:name=".Profile" />
        <activity android:name=".Employee_SignUp" />
        <activity android:name=".Employer_SignUp" />
        <activity android:name=".Reset_password_activity" />
        <activity android:name=".Employer_login" />
        <activity android:name=".Employee_Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>